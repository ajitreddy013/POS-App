services:
  - type: web
    name: pos-app-nqsm
    runtime: node
    rootDir: whatsapp-relay
    buildCommand: npm install
    startCommand: node --max-old-space-size=150 index.js
    plan: free
    envVars:
      - key: PORT
        value: 10000
